<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../../Dictionaries/Images.xaml" />
    </ResourceDictionary.MergedDictionaries>


    <Style x:Key="_imageBase" TargetType="{x:Type Image}">
        <Setter Property="Height" Value="16" />
        <Setter Property="Margin" Value="2 0 2 0" />
        <Setter Property="Stretch" Value="Uniform" />
        <Setter Property="Width" Value="16" />
    </Style>

    <Style x:Key="HeaderIcon" TargetType="{x:Type Image}" BasedOn="{StaticResource _imageBase}">
        <Setter Property="HorizontalAlignment" Value="Right" />
        <Setter Property="Margin" Value="16 -32 0 0" />
        <Setter Property="Source" Value="{StaticResource CloudRefresh-d16p0-black}" />
    </Style>


    <Style TargetType="{x:Type Image}" BasedOn="{StaticResource _imageBase}" />

    <Style TargetType="{x:Type TextBlock}">
        <Setter Property="Margin" Value="2 0 2 0" />
    </Style>

</ResourceDictionary>