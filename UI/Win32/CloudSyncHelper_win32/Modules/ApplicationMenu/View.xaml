<UserControl x:Class="XElement.CloudSyncHelper.UI.Win32.Modules.ApplicationMenuUC" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" 

             xmlns:local="clr-namespace:XElement.CloudSyncHelper.UI.Win32.Modules.ApplicationMenu" 
             xmlns:modules="clr-namespace:XElement.CloudSyncHelper.UI.Win32.Modules">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="./Styles.xaml" />
                <ResourceDictionary Source="../../Dictionaries/Resources.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <local:CeilingConverter x:Key="_ceilingConverter" />
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border 
                Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" 
                Width="{Binding ElementName=_tabControl, 
                                Path=Items[0].ActualWidth, 
                                Converter={StaticResource _ceilingConverter}}" 
                Style="{StaticResource LeftStripe}" 
        />

        <Button 
                Grid.Column="0" Grid.Row="0" 
                Command="{Binding Path=Hide}" 
                Style="{StaticResource BackButton}" 
        />

        <TabControl x:Name="_tabControl" 
                    Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1">
            <TabItem Header="{Binding Path=Localization.ApplicationMenu_About, 
                                      Source={StaticResource XamlLocalization}}">
                <modules:AboutUC DataContext="{Binding Path=AboutVM}" />
            </TabItem>
            <TabItem Header="bla-eriuhireguihergi">bla content</TabItem>
        </TabControl>
    </Grid>

</UserControl>
