<UserControl x:Class="XElement.CloudSyncHelper.UI.Win32.Modules.ProgramDetailsView" 
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 

             xmlns:local="clr-namespace:XElement.CloudSyncHelper.UI.Win32.Modules" 

             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../../Dictionaries/Images.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <StackPanel Orientation="Vertical">
        <StackPanel 
                    Orientation="Horizontal">
            <Image 
                    Source="{StaticResource Settings-02-16}" 
                    VerticalAlignment="Top" 
            />
            <TextBlock Text="Konfigurationen:" />
            <local:SupportedOSs 
                                DataContext="{Binding Path=SupportedOSsVM, Mode=OneWay}" 
            />
        </StackPanel>

        <local:PathMap 
                        DataContext="{Binding Path=PathMap, Mode=OneWay}" 
        />
    </StackPanel>

</UserControl>
