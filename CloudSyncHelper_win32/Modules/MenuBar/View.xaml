<UserControl x:Class="XElement.CloudSyncHelper.UI.Win32.Modules.MenuBarUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../../Dictionaries/Resources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <StatusBar>
        <StatusBarItem 
                        HorizontalAlignment="Right">
            <Button 
                    Command="{Binding Path=ShowAbout, Mode=OneWay}" 
                    Content="{Binding Path=Localization.MenuBar_AboutButton, 
                                      Source={StaticResource XamlLocalization}}" 
                    ToolTip="{Binding Path=Localization.MenuBar_AboutButton, 
                                      Source={StaticResource XamlLocalization}}" 
            />
        </StatusBarItem>
    </StatusBar>

</UserControl>
